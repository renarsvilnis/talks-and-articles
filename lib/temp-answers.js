/**
 * Object that temporary holds answers that is shared across cli questions.
 * Just because with inquirer library I can't prefill answers.
 * Object must be mutated in a "mutability" way
 * @type {Object}
 */
module.exports = {};
